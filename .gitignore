# ============ Python ============
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.pytest_cache/
.hypothesis/
test_results/

# Translations
*.mo
*.pot

# Django stuff (not used here, but harmless)
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff (not used here, but harmless)
instance/
.webassets-cache

# Scrapy stuff (not used here, but harmless)
.scrapy

# Sphinx documentation
doc/_build/

# PyBuilder
target/

# Jupyter Notebook
.ipynb_checkpoints
*.ipynb

# IPython
profile_default/
ipython_config.py

# pyenv
.python-version

# pipenv
Pipfile.lock

# poetry
poetry.lock

# pdm
.pdm.toml
.pdm-python

# PEP 582
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.envrc
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm / IntelliJ / WebStorm
.idea/

# ============ Project-Specific ============
# Datasets and model weights (large files; do not commit)
/data
/models

# Runs: commit lightweight execution assets, keep datasets/models/checkpoints out.
#
# Allowed under runs:
# - run-level docs/status files: execution_plan.md/json, execution_log.md,
#   status.json, current_step.txt, pid.txt, etc.
# - configs/, scripts/, logs/
#
# Still ignored under runs:
# - datasets and generated train/eval JSONL files
# - model/tokenizer/backbone caches
# - all artifacts, checkpoints, safetensors and offline packages
# - generated images, parquet/csv/zips and other bulky artifacts
/runs/**
!/runs/**/
!/runs/*/*.md
!/runs/*/*.json
!/runs/*/*.txt
!/runs/*/*.log
!/runs/*/configs/**
!/runs/*/scripts/**
!/runs/*/logs/**

/runs/**/data/**
/runs/**/artifacts/**
/runs/_*/**
/runs/**/*.jsonl
/runs/**/*.parquet
/runs/**/*.csv
/runs/**/*.safetensors
/runs/**/*.bin
/runs/**/*.pt
/runs/**/*.pth
/runs/**/*.onnx
/runs/**/*.png
/runs/**/*.jpg
/runs/**/*.jpeg
/runs/**/*.zip
/runs/**/*.tar.gz

# ML artifacts (large files; do not commit)
artifacts/
*.safetensors
*.bin
*.pt
*.pth
*.onnx

# Datasets (large; download via scripts/data)
data/raw/
data/mpid-v1/images/
data/mpid-v1-zerowidth/images/
data/mpid-v1-crossmodal/images/
data/mpid-v1/*.parquet
data/mpid-v1-zerowidth/*.parquet
data/mpid-v1-crossmodal/*.parquet

# OCR / CLIP model caches (downloaded at runtime)
.ocr_cache/
.clip_cache/
.paddleocr/

# Experiment logs
mlruns/
wandb/
logs/
*.log

# Offline package output (generated by scripts/package_offline.py).
# mpid_offline*/ glob matches both legacy (mpid_offline) and versioned
# (mpid_offline_v2/) build outputs. These are large (≈1 GB with
# backbone weights) and intended to be published as GitHub Release
# assets, not committed to git.
mpid_offline*/
*.tar.gz
*.zip

# Generated reports
report/figures/*.png
report/ci_*.json
report/eval_*.json

# Demo screenshots (generated by demo/smoke_pipeline.py and the Gradio app)
demo/screenshots/

# Keep lightweight run assets visible even though broad logs/artifacts rules
# above ignore similarly named directories elsewhere in the repo.
!/runs/**/
!/runs/*/*.md
!/runs/*/*.json
!/runs/*/*.txt
!/runs/*/*.log
!/runs/*/configs/**
!/runs/*/scripts/**
!/runs/*/logs/**

# Final hard excludes for run-local bulky or reproducible assets.
/runs/**/data/**
/runs/**/artifacts/**
/runs/_*/**
/runs/**/*.jsonl
/runs/**/*.parquet
/runs/**/*.csv
/runs/**/*.safetensors
/runs/**/*.bin
/runs/**/*.pt
/runs/**/*.pth
/runs/**/*.onnx
/runs/**/*.png
/runs/**/*.jpg
/runs/**/*.jpeg
/runs/**/*.zip
/runs/**/*.tar.gz

# Jupyter / IPython notebooks in research
notebooks/.ipynb_checkpoints/

# ============ OS / Editor ============
# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Linux
*~
.fuse_hidden*
.Trash-*
.ntfs-*
.dbus

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# VS Code (project root)
.vscode/
!.vscode/settings.json
!.vscode/extensions.json

# Sublime Text
*.sublime-project
*.sublime-workspace

# Vim / Emacs
*.swp
*.swo
*~
.\#*
\#*\#
.\#*

# Editor / tool backup files (auto-save, manual snapshots, etc.)
*.bak
*.bak[0-9]
*.backup
*.orig
*.rej
*.tmp
