# MPID — Multimodal Prompt Injection Detection
# Locked versions for macOS (Apple Silicon, MPS) and x86 (CPU) reproducibility.
# Pinned to versions known to support SmolVLM-500M with 4-bit quantization on MPS.

# --- Core DL stack ---
# torch 2.5+ requires macOS 13+; we still support 12.5+ for older M1 machines.
# Pinned to 2.4.1 / 0.19.1 which is the last release with macOS 12.5 MPS support.
torch>=2.4.1,<2.5
torchvision>=0.19.1,<0.20
transformers>=4.45,<4.50
tokenizers>=0.20,<0.22
safetensors>=0.4

# --- PEFT / 4-bit / accelerate ---
peft>=0.11,<0.15
accelerate>=0.34,<1.0
# bitsandbytes 4-bit path:
# - x86 + CUDA : >=0.43 (full feature set)
# - macOS arm64: only <=0.42.0 wheels available; for MPS we fall back to mlx or fp16
# We pin >=0.42,<0.46 so it works on both platforms.
bitsandbytes>=0.42,<0.46

# Optional: mlx is the MPS-friendly 4-bit path on Apple Silicon.
# Not installed by default on x86. Install with: pip install -r requirements-mlx.txt


# --- Datasets / metrics ---
datasets>=2.20,<3.0
scikit-learn>=1.4,<1.7
numpy>=1.24,<2.1
pandas>=2.0,<3.0
pillow>=10.0,<12.0

# --- Misc ---
pyyaml>=6.0
tqdm>=4.66
huggingface_hub>=0.25
sentencepiece>=0.2
protobuf>=4.25

# --- Dev / test (optional) ---
pytest>=8.0
pytest-cov>=5.0
black>=24.0
ruff>=0.5
